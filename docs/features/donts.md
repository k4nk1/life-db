# やらないこと

**エントリーのフィールド**
| フィールド | 型 | 備考 |
|---|---|---|
| 内容 | string | 必須 |
| 削除日時 | datetime | 削除時に自動記録 |

- 上部に週セレクタ（月曜〜日曜）、左右矢印で週移動
- 各エントリーに対して選択中の週の振り返り（文字列）を記録・閲覧できる
- 振り返りは週ごとに別データとして保存される
- 各エントリーの右端に削除ボタン
- 削除は論理削除: 削除日時を記録し、選択中の週が削除日時より前なら表示、削除日時以降なら非表示
